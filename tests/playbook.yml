---
- hosts: all
  gather_facts: False
  roles: [debian-bootstrap]
  post_tasks:
      - name: Test task with an unprivileged user
        become: True
        become_user: nobody
        command: whoami
        changed_when: False
        when: ansible_connection == 'ssh'
