---
- hosts: openbsd_bootstrap
  gather_facts: false
  roles: [openbsd-bootstrap]

- hosts: debian_bootstrap
  gather_facts: false
  roles: [debian-bootstrap]

- hosts: all
  strategy: free
  roles:
    - role: example
